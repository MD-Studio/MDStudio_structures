sudo: false
language: python

os:
  - linux
python:
  - "2.7"
  - "3.5"
  - "3.6"

env:
  - CODECOV_TOKEN="09d5b816-211b-4ca8-b833-2dfaea31afb1"

install:
  # install miniconda
  - wget https://repo.continuum.io/miniconda/Miniconda2-latest-Linux-x86_64.sh -O miniconda.sh
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no --set auto_update_conda False

  # install (test) dependencies
  - (git clone git://github.com/MD-Studio/MDStudio.git --branch master --single-branch && pip install -e MDStudio/mdstudio)
  - conda install -c rdkit rdkit
  - conda install -c openbabel openbabel 
  - pip install "https://github.com/cinfony/cinfony/tarball/master#egg=cinfony-1.2"
  - pip install codecov
  
  # install library
  - pip install .[test]
  
script:
  - (cd tests && coverage run --branch -m unittest discover -vvv)

# Push the results back to codecov
after_success:
  - codecov